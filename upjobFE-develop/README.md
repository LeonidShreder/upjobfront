Frontend application for Upjob
------------------------------


#### Installation instructions:

In your terminal write:

`cd <project_directory>`

Install all required dependencies:

`npm install`

then

`npm run build`

`npm start`

All requirements and scripts are in **package.json** file.